#include <stdio.h>

/**
 * main - entry point of the program
 *
 * Return: exit status of the program
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
