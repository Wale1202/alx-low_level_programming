#include "main.h"
/**
 * mul - checks for checks for a digit (0 through).
 * @a: a - parameter 1
 * @b: b - parameter 2
 * Return: a * b
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
